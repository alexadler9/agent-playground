package domain.statefulagent.model

/**
 * Набор проектных инвариантов.
 *
 * Инварианты загружаются отдельно и передаются агентам как ограничения,
 * которые имеют приоритет над пользовательским запросом.
 */
data class InvariantSet(
    /** Markdown-текст с инвариантами проекта. */
    val content: String,
)